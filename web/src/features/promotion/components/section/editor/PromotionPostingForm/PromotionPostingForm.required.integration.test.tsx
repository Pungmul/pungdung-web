import { describe, it } from "vitest";

describe("PROMO-008 | 홍보 폼 작성 - 필수값 누락", () => {
  it.skip("필수 항목 하나 이상 미입력", () => {
    // 1. 홍보 폼 작성 화면 진입
    // 2. 필수 항목 하나 이상 미입력
    // 3. [저장] 또는 [게시] 버튼 선택
    // 4. 누락 필드 상태 확인
    // 제품 게시 액션은 필드별 클라이언트 검증 문구가 없음
    // toast-ui 에디터/지도 주소 선택에 의존
  });
});
