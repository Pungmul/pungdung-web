import { describe, it } from "vitest";

describe("LGT-050 | 번개 생성 장소 - 검색 결과 없음", () => {
  it.skip("존재하지 않는 장소 검색어 입력", () => {
    // 1. 존재하지 않는 장소 검색어 입력
    // 2. 검색 실행
    // 3. 검색 결과 영역 확인
    // 4. [다음] 버튼 상태 확인
    // LocationMapPicker는 카카오 Places SDK에 의존
    // 빈 검색 문구 "검색 결과가 없어요"도 제품에 없음
  });
});
