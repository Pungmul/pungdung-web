export const READ_SIGN_CATCH_UP_DELAY_MS = 150;
export const MAX_ATTEMPTS = 5;
/** post-entry readSign 전 messageList·socket 병합이 한 박자 끝날 때까지 대기 */
export const POST_ENTRY_READ_SIGN_STABILIZE_MS = 100;
/** foreground gap reconciliation throttle (visibility/pageshow/reconnect) */
export const FOREGROUND_RECONCILE_THROTTLE_MS = 1_000;
